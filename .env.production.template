# ═════════════════════════════════════════════════════════════════
# PRODUCTION ENVIRONMENT VARIABLES - RENDER.COM
# ═════════════════════════════════════════════════════════════════
#
# ⚠️ CRITICAL: Set these variables on Render.com immediately
# Do NOT commit this file with actual credentials
#
# Location: Render.com Dashboard > niyzo-backend > Settings > Environment
#
# ═════════════════════════════════════════════════════════════════

# REQUIRED: MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/niyzo?retryWrites=true&w=majority
# Get this from: https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/niyzo?retryWrites=true&w=majority

# Environment: Must be "production" to enforce strict database requirements
NODE_ENV=production

# Port: Render.com automatically sets this, but can be overridden
# PORT=4000

# ═════════════════════════════════════════════════════════════════
# OPTIONAL: Email Configuration (if implementing email sending)
# ═════════════════════════════════════════════════════════════════

# Email Provider: console | nodemailer | sendgrid
# EMAIL_PROVIDER=console

# SMTP Configuration (if using nodemailer)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password
# EMAIL_FROM=noreply@youthsolve.com

# SendGrid Configuration (if using SendGrid)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxx
# EMAIL_FROM=noreply@youthsolve.com

# ═════════════════════════════════════════════════════════════════
# INSTRUCTIONS
# ═════════════════════════════════════════════════════════════════
#
# 1. Create MongoDB Atlas account: https://www.mongodb.com/cloud/atlas
# 2. Create free M0 cluster
# 3. Get connection string from "Connect" button
# 4. Replace "username:password@cluster0" with your actual credentials
# 5. Go to Render.com > niyzo-backend > Settings > Environment
# 6. Add these variables exactly as shown above
# 7. Click "Deploy" to redeploy with new configuration
# 8. Check logs to verify: "✅ MongoDB Connected Successfully"
#
# ═════════════════════════════════════════════════════════════════
